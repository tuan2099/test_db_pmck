'use strict';

/**
 * question-dataset-definition service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::question-dataset-definition.question-dataset-definition');
