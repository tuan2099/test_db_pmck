'use strict';

/**
 * question-dataset-definition router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::question-dataset-definition.question-dataset-definition');
