'use strict';

/**
 * question-attempt-step-data service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::question-attempt-step-data.question-attempt-step-data');
