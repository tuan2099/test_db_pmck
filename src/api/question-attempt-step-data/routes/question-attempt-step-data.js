'use strict';

/**
 * question-attempt-step-data router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::question-attempt-step-data.question-attempt-step-data');
