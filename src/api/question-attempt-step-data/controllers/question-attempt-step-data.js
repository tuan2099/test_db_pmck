'use strict';

/**
 * question-attempt-step-data controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::question-attempt-step-data.question-attempt-step-data');
