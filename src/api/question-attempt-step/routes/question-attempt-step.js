'use strict';

/**
 * question-attempt-step router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::question-attempt-step.question-attempt-step');
